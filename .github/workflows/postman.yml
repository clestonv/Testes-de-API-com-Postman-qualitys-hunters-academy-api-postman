# This is a basic workflow to help you get started with Actions

name: Testes API Postman

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:

  # This workflow contains a single job called "build"
  build:
    name: Testes API
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a set of commands using the runners shell
      - name: Etapa 01 - Verificação versão do Node e Newman
        run: 
          newman --version && node --version
              
      # Runs a single command using the runners shell
      - name: Etapa 02 - Instalando Newman e Html Extra Reporter
        run: sudo npm install -g newman-reporter-htmlextra  && sudo npm i -g newman
    
      # Runs a set of command using the runners shell
      - name: Etapa 03 - Rodando as Collections de Testes
        run: newman run ./hunters-collection.json -e ./hunters-env.json -r htmlextra --reporter-htmlextra-displayProgressBar ./results/Report.html

      # Runs a set of command using the runners shell
      - name: Etapa 04 - Salvando o Resultado dos Testes
        uses: actions/upload-artifact@v4
        with:
             name: report
             path: ./results/Report.html
             
